
import 'package:flutter/material.dart';

class A2ZTeacher extends StatelessWidget {
  const A2ZTeacher({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(

    );
  }
}
